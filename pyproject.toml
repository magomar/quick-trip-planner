[project]
name = "quick-trip-planner"
version = "0.1.0"
description = "Flight connections explorer for planning quick trips from any airport"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
]

[project.scripts]
serve = "quick_trip_planner.main:run"
